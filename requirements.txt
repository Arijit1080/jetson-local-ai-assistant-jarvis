# Core
numpy<2                # tflite-runtime 2.14 (used by openWakeWord) doesn't support numpy 2 yet
flask==3.1.3
httpx                  # talks to Ollama

# Speech I/O
sounddevice==0.5.5     # PortAudio bindings — record/playback
soundfile              # WAV read/write
webrtcvad-wheels       # WebRTC VAD (works where Silero fails on AGC codecs)

# Wake word
openwakeword==0.6.0
# tflite-runtime is pulled by openwakeword

# STT
faster-whisper==1.2.1  # CTranslate2-backed Whisper
# NOTE: on Jetson aarch64 with CUDA, `ctranslate2` needs to come from either
#   (a) source build with -DWITH_CUDA=ON  (see README "from scratch"), or
#   (b) the Jetson AI Lab index:
#       pip install ctranslate2 --extra-index-url https://pypi.jetson-ai-lab.io/jp6/cu126
# The plain PyPI wheel is CPU-only on aarch64.

# TTS
piper-tts==1.4.2
pathvalidate          # piper dep

# onnxruntime — Jetson GPU wheel comes from jetson-ai-lab.io, see README
# pip install onnxruntime-gpu --extra-index-url https://pypi.jetson-ai-lab.io/jp6/cu126
